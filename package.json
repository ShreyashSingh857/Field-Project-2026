{
  "name": "field-project",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "backend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:user": "npm run dev --workspace=user-app",
    "dev:worker": "npm run dev --workspace=worker-app",
    "dev:admin": "npm run dev --workspace=admin-app"
  },
  "dependencies": {
    "i18next": "^25.10.2"
  }
}
